# Markdown docs (internal — keep only README.md + admin runbooks + admin index)
*.md
!README.md
!docs/admin/README.md
!docs/admin/runbooks/*.md

# Design template drops (large standalone HTML mockups, not part of the app)
MotionMax*.html
*_standalone_.html

# Migration scripts (contain DB exports with sensitive data)
scripts/export/
scripts/export-source-db.mjs
scripts/export-large-tables.mjs
scripts/fix-sql-strings.mjs
scripts/import-to-target.mjs
scripts/migrate-storage.mjs

# Environment files — NEVER commit secrets (single authoritative block)
.env
.env.local
.env.*.local
worker/.env
worker/.env.local

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
marketing-dist
marketing/.astro
*.local
*.md
!README.md
!docs/admin/README.md
!docs/admin/runbooks/*.md
!docs/admin/security-audit.md
!docs/admin/perf-verification.md
!ADMIN_REBUILD_CHECKLIST.md
!supabase/migrations/README.md

# Source maps (never commit — may contain original source; Sentry uploads them from CI only)
dist/assets/*.js.map
dist/assets/*.css.map

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# Archive directory — historical one-off scripts, not for production use
archive/

scripts/check-queue.mjs
scripts/clear-stuck-job.mjs
scripts/fetch-project-script.mjs
scripts/repair-hypereal-images.mjs
scripts/repair-imageUrls.mjs
scripts/clear-stuck-job.mjs
.claude/worktrees/elated-williamson
.claude/worktrees/busy-lederberg
.claude/worktrees/busy-lederberg

# Studio Zero audit reports — one source of truth lives in studio-zero/shared_context/audits/
.audits/

# B-5 fix (2026-05-10): backup of pre-fix byte-identical PNG bundle
public/_archive-pre-b5-fix/
ADMIN_REBUILD_CHECKLIST.md
AUTOPOST_ENV.md
AUTOPOST_PLAN.md
AUTOPOST_ROADMAP.md

# Voice locale QA pipeline (scripts/qa-voice-locales.mjs)
# - Audio outputs are big and regeneratable → ignore
# - Test paragraph .txt files ARE tracked (repo content, see exception below)
# - The runbook + paragraph README are tracked via the existing allowlist for docs/
qa-output/
!scripts/qa-test-paragraphs/
!scripts/qa-test-paragraphs/*.txt
!scripts/qa-test-paragraphs/README.md
!docs/voice-locale-qa-runbook.md
