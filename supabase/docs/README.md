# Supabase Reference Documents

Files in this directory are **reference-only** and are not processed by the Supabase CLI.

## 001_full_schema.sql

A consolidated snapshot of the complete database schema at a point in time.
Useful as a human-readable reference but **not** the source of truth.

The source of truth is the ordered set of files in `supabase/migrations/`.
